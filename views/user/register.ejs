<!DOCTYPE html>
<html lang="en">


<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registrate en Mundo Mascotas</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@2.5.0/fonts/remixicon.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/register.css">
</head>

<body>
    <div class="form">
        <span class="form__title">Registrate en Mundo Mascotas!</span>
        <form action="/login/register" method="POST" enctype="multipart/form-data">
            <div class="form__input">
                <i class="ri-user-line"></i>
                <input type="text" name="Nombre" placeholder="Nombre" id="Nombre" value="<%= locals.old && old.Nombre ? old.Nombre : '' %>" required>
                <span class="bar"></span>
                <% if(locals.errorsregister && errorsregister.Nombre){ %>
                    <p style="color:red"> <%=errorsregister.Nombre.msg%> </p>
                <%}%>
            </div>
            <div class="form__input">
                <i class="ri-user-line"></i>
                <input type="text" name="Apellido" placeholder="Apellido" id="Apellido" value="<%= locals.old && old.Apellido ? old.Apellido : '' %>"required>
                <span class="bar"></span>
                <% if(locals.errorsregister && errorsregister.Apellido){ %>
                    <p style="color:red"> <%=errorsregister.Apellido.msg%> </p>
                <%}%>
            </div>
            <div class="form__input">
                <i class="ri-user-line"></i>
                <input type="text" name="DNI" placeholder="DNI"  id="DNI" value="<%= locals.old && old.DNI ? old.DNI : '' %>"required>
                <span class="bar"></span>
                <% if(locals.errorsregister && errorsregister.DNI){ %>
                    <p style="color:red"> <%=errorsregister.DNI.msg%> </p>
                <%}%>
            </div>
            <div class="form__input">
                <i class="ri-user-line"></i>
                <input type="text" name="Telefono" placeholder="Telefono" id="client_telephone" value="<%= locals.old && old.Telefono ? old.Telefono : '' %>"required>
                <span class="bar"></span>
                <% if(locals.errorsregister && errorsregister.Telefono){ %>
                    <p style="color:red"> <%=errorsregister.Telefono.msg%> </p>
                <%}%>
            </div>
            <div class="form__input">
                <i class="ri-mail-line"></i>
                <input type="email" name="Email" placeholder="Email" id="Email" value="<%= locals.old && old.Email ? old.Email : '' %>"required>
                <span class="bar"></span>
                <% if(locals.errorsregister && errorsregister.Email){ %>
                    <p style="color:red"> <%=errorsregister.Email.msg%> </p>
                <%}%>
            </div>
            <div class="form__input">
                <i class="ri-lock-line"></i>
                <input type="password" name="Password" placeholder="Contraseña" id="Password"  required>
                <span class="bar"></span>
                <% if(locals.errorsregister && errorsregister.Password){ %>
                    <p style="color:red"> <%=errorsregister.Password.msg%> </p>
                <%}%>
            </div>
            <div class="form__input">
                <i class="ri-lock-line"></i>
                <input type="password" name="Password2" placeholder="Confirme contraseña" id="Password2" required>
                <span class="bar"></span>
                <% if(locals.errorsregister && errorsregister.Password2){ %>
                    <p style="color:red"> <%=errorsregister.Password2.msg%> </p>
                <%}%>
            </div>
            <div class="form__input">
                <i class="ri-user-line"></i>
                <input type="file" name="Avatar" placeholder="Adjuntar imagen de perfil" id="Avatar" accept= "pdf, jpeg, jpg" required>
                <span class="bar"></span>
                <% if(locals.errorsregister && errorsregister.Avatar){ %>
                    <p style="color:red"> <%=errorsregister.Avatar.msg%> </p>
                <%}%>
            </div>
            <div class="form__input">
                <label for="idUsersCategory">Seleccionar tipo de usuario:</label>
                <select class="selector" required aria-label="select example" name="idUsersCategory">
                  <% for (let i=0; i < categoriaUsers.length; i++){ %>
                  <option value="<%= categoriaUsers[i].idUsersCategory%>" name="idUsersCategory">
                    <%= categoriaUsers[i].NombreCategoriaUsuario%>
                  </option>
                  <% } %>
                </select>
                <span class="bar"></span>
              </div>
            <button type="submit" class="form__button">Registrarme</button>
            <span class="form__switch">
                Ya tenes cuenta?? <a href="/login">Login</a>
            </span>
            <span class="form__switch">Regresar al <a href="/">inicio</a></span>
        </form>
    </div>
</body>

</html>